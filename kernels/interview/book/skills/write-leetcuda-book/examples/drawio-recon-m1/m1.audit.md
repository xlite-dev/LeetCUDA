# m1.audit.md — drawio-reconstruction smoke test (2026-09-11)

Reference: m1.png (copy of drawio-reconstruction/examples/m1.png)
Workdir: LeetCUDA/kernels/interview/book/.tmp/drawio-skill-test/
Export: DRAWIO_PATH=/usr/local/bin/drawio-headless (xvfb + --no-sandbox wrapper, verified)

## Visible-element inventory (coordinator 初判, Producer 需用视觉核对精确文本/颜色)

| id | 区域(bbox 约略) | 描述 | medium | status |
|---|---|---|---|---|
| canvas | 全图 | 浅色背景 slide，顶部标题区+左侧大面板+中部面板+右侧分层列 | native | pending |
| title | 左上 | 大标题「大模型时代的存储分层」深色粗体，标题左侧有一个绿色圆形装饰图标 | native + icon | pending |
| subtitle | 标题下 | 灰色小字说明行（Producer 视觉核对原文） | native | pending |
| panel-l1 | 左侧大圆角面板 | 「L1 本地内存 + KV Cache」标题 + 内部多个圆角 chip（GPU 显存/高频数据等，文本待核） | native | pending |
| panel-l2 | 中部面板 | 「L2 高速网络存储层」+ chips | native | pending |
| panel-l3 | 右侧分层列 | 「L3 慢速网络存储层」多行，每行 = 存储图标 + 标签（热/温/冷数据等，文本待核） | native + icons | pending |
| icons | panel-l3 各行 + 标题左侧 | 数据库/服务器/硬盘/云等紧凑图标族（风格统一、扁平双色） | crop or SVG（Icon Producer 决策） | pending |
| arrows | 面板间 | 红色虚线双向箭头 L1↔L2↔L3 | native dashed connectors | pending |
| footer | 底部 | 小字注脚（Producer 核对） | native | pending |
| watermark | — | 本图为 skill 示例，无明显水印；**正式管线中知乎图的水印一律标记为非内容元素** | n/a | n/a |

## Review rounds

| phase | artifact_version | producer_id | reviewer_id | verdict | fix_ids | sha256 |
|---|---|---|---|---|---|---|
| smoke | v1 | coordinator-main (user 指定单 agent 模式，跳过 task agent) | coordinator-main 自审（视觉对比） | smoke-pass（工具链验证；保真=结构级，小字待逐字核对） | — | 见 m1.drawio |

## Smoke test 结论（2026-09-11）

1. **工具链全通**：deb 安装（v31.4.5，Ubuntu noble 无 apt 官方包，须用 GitHub releases deb）→ wrapper `/usr/local/bin/drawio-headless`（xvfb-run + --no-sandbox）→ `export_drawio.py`（DRAWIO_PATH）→ `check_drawio.py`（81 cells 全原生、0 image/svg）。
2. **CJK 渲染正常**：headless 导出中文无豆腐块（fontconfig fallback 到 Noto CJK）——成书管线关键前提验证通过。
3. dbus 报错为容器噪音，不影响导出。
4. **执行模式**（用户拍板）：不派 task agent（闭环太慢易卡）；由主 agent 直接完成 inventory+重建+导出+自审。正式管线中独立 Reviewer 环节按 skill 契约保留或由主 agent 换会话复查，audit 如实记录模式。
5. 保真度分级：本次为「结构级」（布局/色彩/图标/箭头/主要文本正确；小字为合理占位）。正式重建（RFC-H）须对 `_prod_crops/` 放大图逐字核对后替换占位文本。
